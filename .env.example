# Supabase
SUPABASE_URL=https://your-supabase-url
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Existing AI gateway (fallback)
LOVABLE_API_KEY=your-lovable-api-key

# IBM Granite (optional)
# Set these to enable IBM Granite as the AI provider. If both IBM vars are present, the function will use IBM Granite.
IBM_GRANITE_API_KEY=your-ibm-api-key
# Full API URL for your IBM Granite endpoint. Example (replace with your region/instance path):
# https://api.us-south.watsonx.ai/v1/generation
IBM_GRANITE_API_URL=https://your-ibm-granite-endpoint
# Optional: override the model identifier if needed
IBM_GRANITE_MODEL=granite-1

# Frontend (publishable) keys
VITE_SUPABASE_URL=https://your-supabase-url
VITE_SUPABASE_PUBLISHABLE_KEY=your-publishable-key
